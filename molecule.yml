---
driver:
  name: docker
docker:
  containers:
#    - name: ansible-role-vim-1404
#      image: ubuntu
#      image_version: 14.04
#      volume_mounts:
#        - ${MOLECULE_APTPROXY_PATH}:/etc/apt/apt.conf.d/00aptproxy

    - name: ansible-role-vim-1604
      image: ubuntu
      image_version: 16.04
      volume_mounts:
        - ${MOLECULE_APTPROXY_PATH}:/etc/apt/apt.conf.d/00aptproxy

ansible:
  playbook: tests/playbook.yml
  group_vars:
    all:
      vim_user: ${MOLECULE_VIM_USER}
